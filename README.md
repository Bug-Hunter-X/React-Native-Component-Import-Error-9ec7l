# React Native Component Import Error

This repository demonstrates a common error in React Native: incorrect component imports.  The `bug.js` file showcases the incorrect import, while `bugSolution.js` provides the corrected version. This issue often arises due to typos, incorrect directory structures, or confusion over file extensions (.js vs .jsx).